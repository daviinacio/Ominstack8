{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://daviinacio.com.br/tindev",
  "dependencies": {
    "axios": "^0.21.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router-dom": "^5.0.1",
    "socket.io-client": "^2.2.0"
  },
  "scripts": {
    "dev": "pm2 start yarn --interpreter bash --name pomodoro -- start",
    "start": "react-scripts start",
    "build": "react-scripts build && cp build/index.html build/404.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
